This folder contains global SCSS component partials.

Files here define shared CSS classes for buttons, cards, inputs, modals, tables, badges, and similar UI patterns. They are imported through the global styles entry points.

Do not confuse this folder with `src/app/components`, which contains Angular components.
