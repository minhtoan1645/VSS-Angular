This folder contains global SCSS utility partials.

Files here define shared CSS classes for buttons, cards, inputs, modals, tables, badges, and similar UI patterns. They are imported through `src/styles.scss`.

Do not confuse this folder with `src/app/shared/components`, which contains Angular components. Before deleting a partial, verify its classes are no longer used by native template elements or page-specific styles.
